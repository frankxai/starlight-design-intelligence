# Hermes — liked references

Before visual or marketing generation:

1. Read `brand-image-system/liked-references/catalog.json`.
2. If Frank pointed at a site/screenshot, match or ingest. Do not invent handles.
3. Slack image 403 → ask for inbox drop or URL. Do not crawl Pictures/Phone Link.
4. Use builtin `web` / `browser` / `vision` on catalog URLs. Codex is optional, not required.
5. Plugins to use: `image_gen/xai` only after brand-image-system says generation is allowed; `figma` MCP for file-backed UI; never treat empty `desktop-plugins` as config.
6. Include `AGENTS.fragment.md` rules (no generated logos/exact text as final).

This fragment is the Hermes wiring for the liked-reference library.
